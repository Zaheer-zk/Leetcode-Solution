class Solution {
public:
    bool isSubsequence(string s, string t) {
                int p1 = 0;

        for(int i=0;i<t.size();i++) {
          if(s[p1] == t[i]) {
            p1++;
          }
        }
      if(s.size() == p1)
        return true;
      return false;
    }
};
